apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rook-gitlab.fullname" . }}-test-connection"
  labels:
    {{- include "rook-gitlab.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rook-gitlab.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
